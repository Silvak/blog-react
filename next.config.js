/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['media.graphassets.com', 'img.freepik.com', 'images.unsplash.com'],
  },
}

module.exports = nextConfig
